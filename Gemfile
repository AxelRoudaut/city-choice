source "https://rubygems.org"

# Jekyll 4 for local development (compatible with Ruby 3.3).
# This site uses no plugins: the GitHub Pages builder (Jekyll 3.9)
# produces the same output.
gem "jekyll", "~> 4.3"

# Dependencies removed from Ruby 3.4+ standard library that Jekyll requires.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"

group :jekyll_plugins do
end
