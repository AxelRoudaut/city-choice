source "https://rubygems.org"

# Jekyll 4 pour le développement local (compatible Ruby 3.3).
# Le site n'utilise aucun plugin : le constructeur GitHub Pages (Jekyll 3.9)
# produit exactement le même rendu.
gem "jekyll", "~> 4.3"

# Dépendances retirées de la stdlib Ruby 3.4+, requises par Jekyll.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"

group :jekyll_plugins do
end
