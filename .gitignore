_site/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
Gemfile.lock

# Polices rapatriées par `just fonts` : le site en ligne utilise le CDN Google.
# Décommentez la ligne suivante si vous préférez servir les polices depuis le
# dépôt (il faut alors les committer).
assets/fonts/

# Secrets locaux (clés API) : voir CLAUDE.md > Secrets
.env.local
.env

# Caches de collecte (exports Geod'air, archives GTFS) : régénérables
.cache/
scripts/**/__pycache__/
